version: "3.9"

services:
  sentinel:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../reports:/app/reports
    command: ["--story", "${JIRA_STORY_KEY:-SMOKE-1}"]
