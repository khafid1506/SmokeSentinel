# SmokeSentinel — Python dependencies
# Install with: pip install -r requirements.txt

# --- LLM backends (install only what you use) ---

# Anthropic Claude (optional — skip if using Ollama or LM Studio)
anthropic>=0.25.0

# OpenAI / LM Studio / Groq / vLLM (optional)
openai>=1.0.0

# HTTP client used by OllamaBackend
requests>=2.31.0

# --- Agent orchestration (future stages) ---
langchain>=0.2.0
langchain-anthropic>=0.1.0

# --- Environment ---
python-dotenv>=1.0.0

# --- Dev / testing ---
pytest>=8.0.0
